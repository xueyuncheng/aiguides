# AIGuide Configuration Example
# 复制此文件为 aiguide.yaml 并填写实际的配置值

# Gemini API 配置（必需）
api_key: your_gemini_api_key_here
model_name: gemini-2.0-flash-exp
# base_url: https://xxxx.com # 自定义 Gemini API Base URL (可选)

# 服务器配置
use_gin: true
gin_port: 8080

# Google OAuth 认证配置
# 需要配置以下参数：
# 1. 访问 Google Cloud Console: https://console.cloud.google.com/
# 2. 创建或选择项目
# 3. 启用 Google+ API
# 4. 创建 OAuth 2.0 客户端 ID（Web 应用类型）
# 5. 添加授权重定向 URI: http://localhost:8080/auth/google/callback
# 6. 获取客户端 ID 和客户端密钥

# google_client_id: YOUR_GOOGLE_CLIENT_ID
# google_client_secret: YOUR_GOOGLE_CLIENT_SECRET
# google_redirect_url: http://localhost:8080/auth/google/callback

# JWT Secret 配置
# jwt_secret 用于签名 JWT token，确保用户登录状态持久化
# 如果不配置，系统会在首次启动时自动生成一个安全的随机密钥
# 自动生成的密钥会保存到此配置文件中，确保服务器重启后用户无需重新登录
# jwt_secret: (留空则自动生成)

# 注意事项：
# - JWT Secret 会在首次启动时自动生成（如果未配置）
# - 生成后的 JWT Secret 会自动保存到配置文件
# - 不要随意修改已生成的 JWT Secret，否则现有用户的登录令牌将失效
# - 生产环境应使用 HTTPS 和相应的重定向 URL
# - 保护好 Client Secret 和 JWT Secret，不要提交到版本控制系统
