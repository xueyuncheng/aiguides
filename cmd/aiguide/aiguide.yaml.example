# AIGuide Configuration Example
# 复制此文件为 aiguide.yaml 并填写实际的配置值

# Gemini API 配置（必需）
api_key: your_gemini_api_key_here
model_name: gemini-2.0-flash-exp
# base_url: https://xxxx.com # 自定义 Gemini API Base URL (可选)

# 服务器配置
use_gin: true
gin_port: 8080

# Google OAuth 认证配置
# 需要配置以下参数：
# 1. 访问 Google Cloud Console: https://console.cloud.google.com/
# 2. 创建或选择项目
# 3. 启用 Google+ API
# 4. 创建 OAuth 2.0 客户端 ID（Web 应用类型）
# 5. 添加授权重定向 URI: http://localhost:8080/auth/google/callback
# 6. 获取客户端 ID 和客户端密钥
# 7. 生成 JWT Secret（使用以下命令）：
#    openssl rand -base64 32

# google_client_id: YOUR_GOOGLE_CLIENT_ID
# google_client_secret: YOUR_GOOGLE_CLIENT_SECRET
# google_redirect_url: http://localhost:8080/auth/google/callback
# jwt_secret: YOUR_RANDOM_JWT_SECRET

# 注意事项：
# - JWT Secret 应该是一个强随机字符串，至少 32 字符
# - 生产环境应使用 HTTPS 和相应的重定向 URL
# - 保护好 Client Secret 和 JWT Secret，不要提交到版本控制系统

# 实时信息查询配置（可选）
# 使用 SearXNG 开源搜索引擎聚合服务
# 
# SearXNG 特点：
# - 完全免费，无需 API Key
# - 搜索全网，无域名限制
# - 聚合 Google、Bing、DuckDuckGo 等多个搜索引擎
# - 保护隐私，不追踪用户
#
# 推荐方式：自建 SearXNG 实例（稳定、无速率限制）
# 部署文档：deployments/searxng/README.md
#
# 自建实例配置（推荐）：
# web_search:
#   instance_url: "http://localhost:8888"  # 本地 SearXNG 实例
#
# 使用公共实例（不推荐，可能不稳定）：
# web_search:
#   instance_url: "https://searx.be"       # 公共实例（可能被墙或限流）
#
# 默认配置（无需显式指定）：
# - 搜索语言：zh-CN（中文）
# - 超时时间：30 秒
# - 最大结果数：10

# Exa 搜索配置（可选）
# Exa 是一个神经网络搜索引擎，擅长深度语义搜索，与 SearXNG 互补：
# - SearXNG 适合实时性强的事实查询（如"今天的金价是多少"）
# - Exa 适合需要深度理解、寻找高质量参考资料或直接获取网页正文的任务
#
# 获取 API Key：https://exa.ai/
#
# exa_search:
#   api_key: "your_exa_api_key_here"
