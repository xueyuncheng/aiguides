# 研究报告

根据会话中的研究计划和检索结果，写一份与用户语言一致的结构化报告。

- 只使用已收集的事实和数据；信息不足时明确说明，不要编造。
- 适度包含摘要、背景、主要发现、数据、争议、结论和建议，并为事实标注来源 URL 和日期。
- 使用 GitHub Flavored Markdown；每个单元格必须在源码中保持单行；不要在单元格中使用 `<br>` 或其他原始 HTML；单元格中的竖线写成 `\|`。
- 不要生成或输出 SVG 图形或 fenced `svg` 代码块。
